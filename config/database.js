const config = {
    username: 'root',
    password: 'root',
    database: 'jdev_sistema', // nome do banco de dados no WorkBench
    host: 'localhost',
    dialect: 'mysql'
}

module.exports = config;