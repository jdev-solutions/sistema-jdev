<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/styles/header.css' />
    <link rel="stylesheet" href='/styles/dadosUser.css'>
    <title>dataProgress</title>
    <link rel="shortcut icon" href="./images/base-de-dados.png">
    <link rel="stylesheet" 
          href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <header class="menu">
        <div>
            <p class="usuario-menu"> Veja os dados de: <%= user.name %> </p>
            <hr>
        </div>
    </header>

    <main class="container my-4">
        <div class="card shadow-sm p-4">
            <h1 class="text-center mb-4">Informações do Usuário</h1>
            <ul class="list-group list-group-flush">
                <li class="list-group-item"><strong>Nome:</strong> <%= user.name %></li>
                <li class="list-group-item"><strong>Email:</strong> <%= user.email %></li>
                <li class="list-group-item"><strong>Idade:</strong> <%= user.age %> anos</li>
                <li class="list-group-item"><strong>Data de Nascimento:</strong> <%= user.birthdate %></li>
                <li class="list-group-item"><strong>Telefone:</strong> <%= user.phone %></li>
                <li class="list-group-item"><strong>CPF:</strong> <%= user.cpf %></li>
                <li class="list-group-item"><strong>Função:</strong> <%= user.produtosId %></li>
            </ul>

            <div class="d-flex justify-content-center gap-3 mt-4">
                <a href="/users/editar/<%= user.id %>" class="btn btn-primary w-50"> Editar dados </a>
                <form action="/users/deletar/<%= user.id %>" method="delete" class="w-50">
                    <button type="submit" class="btn btn-danger w-100"> Excluir </button>
                </form>
            </div>
        </div>
    </main>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>